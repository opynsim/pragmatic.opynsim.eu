Example Data
============

Some guides in this handbook contain examples that load resources (e.g. models,
experimental data) from disk. In those cases, we only use resources from this
zip file:

DOWNLOAD LINK

We assume the resources are unzipped into a directory called ``pragmatic_resources``
in your runtime's current working directory. In IDEs like :doc:`pycharm`
